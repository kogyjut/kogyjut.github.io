services:
  - type: web
    name: kogyjut-scraper-api
    runtime: python
    buildCommand: pip install -r requirements.txt && python -m camoufox fetch
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
